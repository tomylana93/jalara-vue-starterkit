---
paths:
    - 'resources/js/components/ui/**'
---

# Ui

## UI primitives are read-only

Files under resources/js/components/ui/ are shadcn primitives. Never hand-edit them. Compose or wrap outside that folder and pass props, slots, and class. Change primitives only via the shadcn CLI (add or upgrade).
